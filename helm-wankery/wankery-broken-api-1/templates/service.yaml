apiVersion: v1
kind: Service
metadata:
  name: {{ include "wankery.fullname" . }}
  labels:
    {{- include "wankery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wankery.selectorLabels" . | nindent 4 }}
