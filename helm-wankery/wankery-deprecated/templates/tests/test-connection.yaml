apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wankery.fullname" . }}-test-connection"
  labels:
    {{- include "wankery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wankery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
